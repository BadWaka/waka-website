{
  "compilerOptions": {  // 编译器选项
    "outDir": "./dist/",  // 输出目录
    "sourceMap": true,  // 生成相应的.map文件
    "module": "commonjs", // 使用commonjs作为模块加载器（事实上无所谓，我们最终使用webpack打包）
    "target": "es5",  // 将编译的JS设置成为es5版本
    "jsx": "react" // 将 .tsx 文件转换成 React的 JavaScript 语法，而不是jsx语法
  },
  "include": [
    "admin/**/*"
  ],
  "exclude": [
    "node_modules"
  ]
}